#include <stdio.h>
#include "src/gameContent.h"
// src/gameContent.h
int main(int argc, char const *argv[]) {
    /* code */

    printf("hello world /n");

    int value = gameMaths(2, 7);

    printf("game main: %i", value);

    return 0;
}
